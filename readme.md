# AoC 2019 solutions in Kotlin (JVM)

See [AoC 2019](https://adventofcode.com/2019/) for puzzles.

Inputs are defined in each day package (usually `data.kt` file, `IO` class).

To run all, use `./gradlew run`
